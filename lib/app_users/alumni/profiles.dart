import 'package:flutter/material.dart';

class AlumniProfile extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Alumni Profile")),
      body: Center(child: Text("Alumni Profile Page")),
    );
  }
}
